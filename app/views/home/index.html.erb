<% if user_signed_in? %>
<h1><%= current_user.email %></h1>
<p><%= current_user.id %></p>
<%= link_to 'Sign Out', destroy_user_oa_session_path, method: :delete, data: {confirm: 'Are you sure?'} %>
<% else %>
<h1>bla bla</h1>
<% end %>